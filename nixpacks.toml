[build]
  command = "bun install && bun run build && mkdir -p public && cp -r dist/client/* public/"

[start]
  command = "srvx --prod public dist/server/server.js"
  port = 3000

[vars]
  NODE_ENV = "production"

[nixpacks]
  node_version = "22"
