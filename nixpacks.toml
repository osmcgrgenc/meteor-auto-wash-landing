[build]
  command = "bun install && bun run build"
  providers = ["node", "bun"]

[start]
  command = "node server-prod.js"
  port = 3000

[vars]
  NODE_ENV = "production"

[nixpacks]
  node_version = "22"
